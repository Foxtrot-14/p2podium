package dht

func (d *DHT) HandleNewNodes(nodes []Node) {
	//Calculate Bucket Number
	//Check whether bucket is full
	//If full check the least responsive node and ping
	//If ping does not response replace
}
